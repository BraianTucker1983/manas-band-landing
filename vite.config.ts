import { defineConfig } from 'vite';

export default defineConfig({
  // Reemplazá 'nombre-del-repositorio' por el nombre exacto que le vayas a poner a tu repositorio en GitHub
  base: '/manas-band-landing/', 
});